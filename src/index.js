import { createRoot } from "react-dom/client";
import App from "./base/06-事件绑定-2";
const container = document.getElementById("root");

// 创建root
const root = createRoot(container);
root.render(<App />);
